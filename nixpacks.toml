[build]
builder = "NIXPACKS"

[phases.setup]
nixPkgs = ["php", "php81Packages.composer", "nodejs"]

[phases.install]
cmds = [
  "php -v",
  "composer -V",
  "composer validate",
  "composer install --ignore-platform-reqs -vvv", # verbose output
  "npm install"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"
